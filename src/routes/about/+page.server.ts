// ============================================================
// /about — אין דף אודות נפרד: כל הטקסט המסביר (מהות האתר, מה יש במאגר,
// שאלות ותשובות + FAQPage) חי ב-/info. מי שמגיע ל-/about (קישור מאתר אחר
// ברשת, ניחוש של גולש או של מנוע חיפוש) מופנה לשם בהפניה קבועה.
// ============================================================
import { redirect } from '@sveltejs/kit';
import type { PageServerLoad } from './$types';

export const load: PageServerLoad = () => {
    throw redirect(301, '/info');
};
